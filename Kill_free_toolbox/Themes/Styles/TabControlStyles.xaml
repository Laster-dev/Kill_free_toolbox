<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!-- TabControl 样式 -->
    <Style TargetType="TabControl">
        <!--<Setter Property="Background" Value="{DynamicResource PrimaryBackground}"/>-->
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TabPanel x:Name="HeaderPanel"
                                  Background="{TemplateBinding Background}"
                                  Grid.Row="0"
                                  IsItemsHost="True"
                                  Margin="0"/>
                        <Border Background="Transparent" Grid.Row="1" Padding="5">
                            <ContentPresenter x:Name="PART_SelectedContentHost"
                                              Margin="0"
                                              ContentSource="SelectedContent"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="TabItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Grid>
                        <!-- 外层 Border 包含内容和底部指示器 -->
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="7"
                                Padding="5,2">
                            <Grid>
                                <!-- Header 内容 -->
                                <ContentPresenter x:Name="ContentSite"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Center"
                                                  ContentSource="Header"/>

                                <!-- 底部选中指示器，默认隐藏 -->
                                <Border x:Name="SelectionIndicator"
                                        Height="2"
                                        Width="20"
                                        VerticalAlignment="Bottom"
                                        HorizontalAlignment="Stretch"
                                        Background="Transparent"
                                        Visibility="Collapsed"
                                        CornerRadius="1"/>
                            </Grid>
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <!-- 选中状态触发：显示底部横线并将其设为 InfoBrush，同时设置前景色 -->
                        <Trigger Property="IsSelected" Value="True">

                            <Setter TargetName="SelectionIndicator" Property="Background" Value="{DynamicResource InfoBrush}"/>
                            <Setter TargetName="SelectionIndicator" Property="Visibility" Value="Visible"/>
                        </Trigger>

                        <!-- 禁用状态触发 -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="SelectionIndicator" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
